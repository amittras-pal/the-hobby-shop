{
  "name": "the-hobbyshop",
  "version": "1.0.0",
  "description": "A Test project to checkout firebase",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cd client && npm run build && cd .. && firebase deploy",
    "deploy-hosting": "cd client && npm run build && cd .. && firebase deploy --only hosting",
    "deploy-functions": "cd client && npm run build && cd .. && firebase deploy --only functions"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amittras-pal/the-hobby-shop.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/amittras-pal/the-hobby-shop/issues"
  },
  "homepage": "https://github.com/amittras-pal/the-hobby-shop#readme"
}
